/**
 * Placeholder component for Expenses UI components.
 * Future implementation will include selectors, totals display, and validation.
 */
export function ExpensesPlaceholder(): JSX.Element {
  return (
    <div className="placeholder-card" data-testid="expenses-components-placeholder">
      <p>
        Shared Expenses UI components will live here. Implement selectors, totals display, and empty
        states in later phases.
      </p>
    </div>
  );
}

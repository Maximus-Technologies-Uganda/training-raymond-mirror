# Journal — 2025-10-20 (Day 1: Bootstrap & All Three CLIs)

## Goals for Today
- [x] Initialize Node.js project with Vitest and ESLint
- [x] Implement Hello CLI with --name and --shout flags (4 tests)
- [x] Implement Stopwatch CLI with start/lap/stop commands (5 tests)
- [x] Implement Temperature Converter CLI with validation (5 tests)
- [x] Verify all 14 tests pass locally and linting is clean
- [x] Create daily journals and documentation

## Prompts I used (10–15)
- "How do I set up Vitest for Node.js ES modules?" → Configured package.json with "type": "module"
- "How do I guard main() to avoid stdout during tests?" → Used import.meta.url check
- "How do I parse command-line arguments in Node.js?" → Built custom parseArgs() loop over process.argv
- "How do I implement a Stopwatch in JavaScript using Date.now()?" → Used instance state (startTime, laps, isRunning)
- "How do I test for thrown errors in Vitest?" → Used expect(() => stopwatch.lap()).toThrow()
- "What are the formulas for Celsius-Fahrenheit conversion?" → C→F: (C × 9/5) + 32, F→C: (F - 32) × 5/9
- "How do I validate enum-like flags in JavaScript?" → isValidUnit() helper checks 'C' or 'F'
- "Should I allow same-unit conversion?" → No, throw error per workbook requirements
- "How do I format milliseconds as 'Xs Yms'?" → Math.floor(ms / 1000) for seconds, ms % 1000 for remainder

## Commands / Steps I tried
- `npm init -y` — Initialized package.json
- `npm install vitest eslint --save-dev` — Added dev dependencies
- `node src/hello/index.js --name Raymond --shout` — Tests custom greeting with shout
- `node src/stopwatch/index.js start && node src/stopwatch/index.js lap && node src/stopwatch/index.js stop` — Full stopwatch flow
- `node src/temperature/index.js --from C --to F 0` — Expected 32
- `node src/temperature/index.js --from F --to C 212` — Expected 100
- `npm test` — All 14 tests passing
- `npm run lint` — ESLint clean after auto-fix

## PRs & CI
- PR: [Link to feature/all-cllis PR or chore/bootstrap PR]
- CI: [Link to CI run screenshot]
- Test count: 14 tests (4 Hello + 5 Stopwatch + 5 Temperature)

## Bugs & Fixes
- Initially forgot to close parseArgs loop properly in Hello CLI → Fixed indentation
- formatTime in Stopwatch initially didn't return a string → Added return statement
- Tests expected pattern /\d+s \d+ms/ → Updated formatTime to match regex
- Temperature conversion results had floating-point precision issues → Added rounding to 2 decimal places
- ESLint indentation errors in switch statement → Used npx eslint --fix

## What I learned (3–5 bullets)
- ES modules in Node.js require guarding main() to avoid side effects during test imports
- Proper separation of business logic (pure functions) from I/O (CLI parsing) is critical for testability
- TDD approach (write test first) ensures coverage of edge cases and error paths
- Floating-point arithmetic requires careful rounding to avoid precision errors
- Small, focused commits and clean code improve development velocity significantly

## Plan for Tomorrow
- Push feature branches and open PRs to development
- Ensure CI checks pass on all PRs
- Update Review Packet with PR links and CI screenshots
- Open capstone PR with evidence documentation
